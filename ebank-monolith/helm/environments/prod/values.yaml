# Production environment — dynamic values managed by CI.
# This file is the ONLY file the Jenkins / GitHub Actions pipeline modifies.
# Static prod config (Vault host, TLS, resources) lives in values-prod.yaml.
#
# Updated automatically on every successful build of the main branch.
# Every change to this file creates a Git commit; Argo CD detects it and syncs.
# Do NOT edit this file manually.
image:
  tag: latest   # CI overwrites this on every deploy
