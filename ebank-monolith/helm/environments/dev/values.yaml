# Dev environment — dynamic values managed by CI.
# This file is the ONLY file the Jenkins / GitHub Actions pipeline modifies.
# Static dev config (Vault host, resources, replicas) lives in values-dev.yaml.
#
# Updated automatically on every successful build of the develop branch.
# Do NOT edit this file manually.
image:
  tag: latest   # CI overwrites this on every deploy
