apiVersion: v1
kind: Service
metadata:
  name: {{ include "ebank-monolith.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ebank-monolith.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ebank-monolith.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
