# Accounts Service — prod environment (Kubernetes) fallback
# (authoritative config served by Config Server)
server:
  port: 8082

spring:
  cloud:
    kubernetes:
      enabled: true
      discovery:
        enabled: true
      config:
        enabled: true

logging:
  level:
    com.ebank.accounts: WARN
    io.r2dbc.postgresql: WARN

error:
  handling:
    show-details: false
    include-stacktrace: false
    include-validation-errors: false
    include-trace-id: true
    trace-id-header: X-Trace-Id
