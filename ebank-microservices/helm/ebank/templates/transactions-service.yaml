{{- if .Values.transactions.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ebank.fullname" . }}-transactions
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ebank.labels" . | nindent 4 }}
    app.kubernetes.io/component: transactions
spec:
  type: ClusterIP
  selector:
    {{- include "ebank.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: transactions
  ports:
    - name: http
      port: {{ .Values.transactions.port }}
      targetPort: http
      protocol: TCP
{{- end }}
